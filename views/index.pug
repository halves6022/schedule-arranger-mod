extends layout

block content
  h1= title
  p Welcome to #{title}
  div
    if user
      a(href="/logout") #{user.username || user.name.givenName || user.name.familyName}(#{user.provider})をログアウト
    else
      a(href="/login") ログイン
